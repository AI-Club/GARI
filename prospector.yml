strictness: medium

test-warnings: false
doc-warnings: true

ignore-paths:
  - docs
  - lib
  - bin

pep8:
  full: true
  options:
    max-line-length: 100

pylint:
  options:
    docstring-min-length: 20
    max-line-length: 100

mccabe:
  run: false

pep257:
  run: false
